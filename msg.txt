refactor(asp): fail loudly instead of silently renegotiating

The JSON-options bail-out in WriteProblemJsonAsync could not fire: IOptions<T>
is registered as an open generic, so GetService never returns null once any
options infrastructure is present. Worse, if it ever had fired it would have
put the response back on the negotiating path this method exists to avoid.
Door 2 (ProblemDetailsActionResult) already used GetRequiredService; door 1
now matches.

The ValidationProblem switch arm was likewise dead: Results.ValidationProblem
returns a ProblemHttpResult carrying HttpValidationProblemDetails, so the
derived type reaches the runtime-typed serializer through the ProblemHttpResult
arm. Confirmed empirically -- the cast holds across all 8,315 tests, including
every validation-failure case.

Drops the now-false CHANGELOG sentence describing the removed fallback.